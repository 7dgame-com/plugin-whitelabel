# Security

请不要在公开 issue 中提交客户配置、token、密码、内部域名或漏洞利用细节。

发现安全问题时，请通过仓库所有者提供的私密安全报告渠道联系维护者。报告中请
包含受影响版本、复现步骤、影响范围和建议修复方式。

域名 `config_json` 会通过公开解析接口下发到 Unity，因此其中的所有内容都必须视为
公开数据。后端会拒绝常见敏感字段，但这不能替代发布前的数据审查。

完整 hostname 和管理端数字 `domainId` 都不是授权凭证。Unity 只应访问 allow-list
中的白牌插件 HTTPS endpoint；插件按显式 hostname 精确候选匹配，并检查域名记录的
启停状态。公开接口不接收 loginKey、组织、JWT 或数据库地址。
