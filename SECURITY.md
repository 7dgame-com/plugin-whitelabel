# Security

请不要在公开 issue 中提交客户配置、token、密码、内部域名或漏洞利用细节。

发现安全问题时，请通过仓库所有者提供的私密安全报告渠道联系维护者。报告中请
包含受影响版本、复现步骤、影响范围和建议修复方式。

组织和域名两份 `config_json` 都会下发到 Unity 客户端，因此其中的所有内容都应
视为公开数据。后端会拒绝常见敏感字段，但这不能替代发布前的数据审查。

数字组织 ID、域名 ID 和二维码 URL 都只是公开定位符，不是权限凭证。Unity 只应
访问受信任的 `yii3-a1` HTTPS origin；A1 后面的插件服务仍会验证组织、域名和组合
三层启用状态。
